apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
spec:
  selector:
    app: openclaw
  type: ClusterIP
  ports:
    - name: gateway
      port: 18789
      targetPort: gateway
    - name: bridge
      port: 18790
      targetPort: bridge
