# ARC Runner Scale Set — bconnect(구 morton) 레포 전용 러너
# githubConfigUrl은 등록 시점 URL(morton) 유지 필수 — scale set은 등록 config URL 스코프로
# 키잉되므로 bconnect로 바꾸면 stored scaleSetId(1)가 404 → 재등록 필요. rename redirect로 작동.
# GitHub App 인증 → SealedSecret으로 관리 (github-app-secret)
githubConfigUrl: "https://github.com/mortonCareer/morton"
githubConfigSecret: github-app-secret

controllerServiceAccount:
  name: arc-controller-gha-rs-controller
  namespace: actions-runner

# scale-from-zero: 워크플로우 대기 중일 때만 Pod 생성
minRunners: 0
maxRunners: 4

# BE CI(ci-api/ci-api-spec)가 Testcontainers-postgresql 사용 → Docker daemon 필요.
# dind: 러너 Pod에 docker-in-docker 사이드카 + DOCKER_HOST 자동 주입.
containerMode:
  type: "dind"

template:
  spec:
    containers:
      - name: runner
        image: ghcr.io/actions/actions-runner:latest
        command: ["/home/runner/run.sh"]
        resources:
          limits:
            cpu: "2"
            memory: 4Gi
          requests:
            cpu: 250m
            memory: 512Mi
