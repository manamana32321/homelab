apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: saemate-postgres-data
  namespace: saemate
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn-ssd
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: saemate-postgres
  namespace: saemate
  labels:
    app: saemate-postgres
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: saemate-postgres
  template:
    metadata:
      labels:
        app: saemate-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: saemate
            - name: POSTGRES_USER
              value: saemate
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: saemate-api-secrets
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 256Mi
              cpu: 500m
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: saemate-postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: saemate-postgres
  namespace: saemate
spec:
  selector:
    app: saemate-postgres
  ports:
    - port: 5432
      targetPort: 5432
