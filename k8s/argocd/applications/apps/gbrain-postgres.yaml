apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gbrain-postgres
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/manamana32321/homelab
    targetRevision: main
    path: k8s/gbrain-postgres/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: gbrain
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
