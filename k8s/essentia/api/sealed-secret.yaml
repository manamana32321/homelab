---
# TODO: 값은 `scripts/reseal-essentia-secrets.sh` 실행으로 채워짐.
# 클러스터 접근 필요 (현재 saemate ns의 Secret에서 plaintext 추출 → essentia ns scope로 re-seal).
# 스크립트가 이 파일을 in-place로 갱신한다.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: essentia-api-secrets
  namespace: essentia
spec:
  encryptedData: {}
  template:
    metadata:
      name: essentia-api-secrets
      namespace: essentia
