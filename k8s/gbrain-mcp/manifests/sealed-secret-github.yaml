---
# Placeholder — user must generate a fine-grained GitHub PAT for manamana32321/brain (read-only)
# and seal it with kubeseal:
#
#   echo -n "ghp_..." | kubeseal --raw \
#     --cert k8s/sealed-secrets/cert.pem \
#     --name gbrain-github-credentials \
#     --namespace gbrain \
#     --scope strict
#
# Replace the placeholder below with the resulting ciphertext.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: gbrain-github-credentials
  namespace: gbrain
spec:
  encryptedData:
    GH_PAT: PLACEHOLDER_REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: gbrain-github-credentials
      namespace: gbrain
