---
# TODO: 값은 `scripts/reseal-essentia-minio-secrets.sh` 실행으로 채워짐.
# saemate ns의 minio-config Secret에서 plaintext 추출 → essentia ns scope로 re-seal.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: minio-config
  namespace: essentia
spec:
  encryptedData:
    config.env: ""
  template:
    metadata:
      name: minio-config
      namespace: essentia
