# TODO: seal with kubeseal
# kubectl create secret generic opencampus-api-secrets \
#   --from-literal=DATABASE_URL=postgresql://opencampus:PASSWORD@opencampus-postgres:5432/opencampus \
#   --from-literal=POSTGRES_PASSWORD=CHANGE_ME \
#   --from-literal=MINIO_ACCESS_KEY=CHANGE_ME \
#   --from-literal=MINIO_SECRET_KEY=CHANGE_ME \
#   --from-literal=OPENAI_API_KEY=CHANGE_ME \
#   --from-literal=CANVAS_ACCESS_TOKEN=CHANGE_ME \
#   --dry-run=client -o yaml | kubeseal -o yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: opencampus-api-secrets
  namespace: opencampus
spec:
  encryptedData: {}
  template:
    metadata:
      name: opencampus-api-secrets
      namespace: opencampus