apiVersion: v1
kind: Service
metadata:
  name: go2rtc
  namespace: frigate
spec:
  selector:
    app: go2rtc
  ports:
    - name: api
      port: 1984
      targetPort: api
    - name: rtsp
      port: 8554
      targetPort: rtsp
