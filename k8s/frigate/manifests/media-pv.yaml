apiVersion: v1
kind: PersistentVolume
metadata:
  name: frigate-media-hdd
spec:
  capacity:
    storage: 200Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    # type: Directory — 경로가 없으면 kubelet이 마운트를 거부한다. immich library-pv.yaml 참조.
    path: /mnt/hdd-seagate-1t/frigate-media
    type: Directory
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - json-server-1
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate-media-hdd
  namespace: frigate
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 200Gi
  volumeName: frigate-media-hdd
