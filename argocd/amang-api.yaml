apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: amang-db
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: amang-db
    server: https://kubernetes.default.svc
  source:
    path: helm/db
    repoURL: https://github.com/skku-amang/main
    targetRevision: main
    helm:
      valueFiles:
        - values.yaml
      parameters:
        - name: image.repository
          value: ghcr.io/skku-amang/db
        - name: image.tag
          value: latest
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
